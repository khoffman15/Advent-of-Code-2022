

# DRAFT IM NOT DONE!


max = 0
max2 = 0    # second greatest calories
max3 = 0    # third greatest
count = 0
for item in data:
    if item == '':
        count = 0       # Reset count skip to next Elf
    else:
        num = int(item) 
        count += num    

    # Finding max values
    if count > max: 
        max = count     # most calories
    elif count > max2:
        max2 = count    # second 
    elif count > max3:
        max3 = count    # third 
